# Plan for #7: Add readiness and liveness probes, run two replicas

## Goal
Update the Kubernetes Deployment so two application pods run and Kubernetes only
routes traffic to pods that pass the application health endpoint. No application
API, configuration, or user-facing behavior changes are required.

## Dependencies (confirmed / plausible / open)
- **confirmed — change: yes:** `k8s/deployment.yaml` Deployment currently has
  `replicas: 1`, exposes the named `http` port on 3000, and has no probes.
  Add `replicas: 2`, plus readiness and liveness probes using `GET /health`
  on the named HTTP container port.
- **confirmed — change: no:** `src/app.ts` already serves `GET /health` with
  HTTP 200 and the existing health payload.
- **confirmed — change: no:** `k8s/service.yaml` selects both replicas through
  the `app: sandbox-app` label and targets the named HTTP port.
- **confirmed — change: no:** `k8s/kustomization.yaml` already includes the
  Deployment.
- **confirmed — change: no:** `src/config.ts`, `k8s/configmap.yaml`, and the
  README environment table have no probe-related variables.
- **confirmed — change: no:** `test/app.test.ts`, `e2e/smoke.spec.ts`, and
  `.github/workflows/ci.yml` already test `/health` and deployment rollout,
  stability, smoke, and browser behavior.
- **plausible — change: no:** Pipeline rollout timing may need adjustment if
  default probe timings delay readiness beyond the existing 90-second rollout;
  verify with the pipeline rather than changing workflow behavior preemptively.
- **open — change: no:** None.

## Tests
- No new unit or browser tests are needed; the existing health unit test,
  browser health test, and pipeline rollout/stability checks cover this change.
- Run `npm run verify`; deployment-specific validation is the pipeline's
  kind-cluster rollout and smoke checks.

## Environment contract
No environment variables are added or changed. `src/config.ts`,
`k8s/configmap.yaml`, and the README table remain unchanged.

## Risks
Probe configuration errors could keep both pods unready or restart healthy pods;
use the existing `/health` path and container port 3000. The pipeline rollout,
two-pod stability check, service smoke test, and browser test could fail if probe
timing or port wiring is incorrect.

## Open questions
1. None; the issue specifies the health endpoint and desired replica count.

## Decisions
1. Proceed without additional open questions; use `GET /health` on the named
   HTTP port for both probes and set the Deployment to two replicas.
